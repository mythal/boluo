import styled from '@emotion/styled';
import { textBase, textColor } from '@/styles/atoms';

export const Text = styled.p`
  color: ${textColor};
  font-size: ${textBase};
  margin: 0;
  padding: 0;
`;

export default Text;
