meta {
  name: Refresh Token
  type: http
  seq: 12
}

post {
  url: {{base_url}}/spaces/refresh_token?id={{space_id}}
  body: json
  auth: inherit
}

params:query {
  id: {{space_id}}
}

body:json {
  {}
}
