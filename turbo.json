{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "noUpdateNotifier": true,
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "generate": { "dependsOn": ["^generate"], "inputs": ["$TURBO_DEFAULT$"] },
    "build": {
      "dependsOn": ["^generate"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**"]
    },
    "//#lang:extract": {
      "inputs": [
        "apps/{site,spa}/**/*.{ts,tsx,js,jsx}",
        "packages/**/*.{ts,tsx,js,jsx}",
        "packages/lang/package.json",
        "!**/{node_modules,out,.next,storybook-static,dist,.turbo}/**/*",
        "!**/*.d.ts"
      ],
      "outputs": ["packages/lang/src/en.json"]
    },
    "lint": { "dependsOn": ["@boluo/icons#generate"] },
    "test": { "dependsOn": ["check"] },
    "check": { "dependsOn": ["^check", "@boluo/icons#generate"] },
    "storybook": {
      "dependsOn": ["^generate"],
      "cache": false,
      "persistent": true,
      "interruptible": true
    },
    "dev": {
      "dependsOn": ["^generate"],
      "cache": false,
      "persistent": true,
      "interruptible": true,
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env.local",
        "$TURBO_ROOT$/.env",
        "$TURBO_ROOT$/.env.local"
      ],
      "env": ["ANALYZE", "BACKEND_URL"]
    },
    "spa#build": {
      "dependsOn": ["^generate", "@boluo/backend-proxy#build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env.local",
        "$TURBO_ROOT$/.env",
        "$TURBO_ROOT$/.env.local"
      ],
      "env": ["ANALYZE", "APP_VERSION", "BACKEND_URL", "GITHUB_SHA"],
      "outputs": ["out/**", ".next/**", "!.next/cache/**"]
    },
    "site#build": {
      "dependsOn": ["^generate"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env.local",
        "$TURBO_ROOT$/.env",
        "$TURBO_ROOT$/.env.local"
      ],
      "env": ["ANALYZE", "APP_VERSION", "BACKEND_URL", "GITHUB_SHA"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "legacy#build": {
      "dependsOn": ["^generate", "@boluo/backend-proxy#build"],
      "env": ["APP_VERSION", "GITHUB_SHA"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env.local",
        "$TURBO_ROOT$/.env",
        "$TURBO_ROOT$/.env.local"
      ],
      "outputs": ["dist/**"]
    },
    "boluo-storybook#build": {
      "dependsOn": ["^generate"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["storybook-static/**"]
    },
    "@boluo/lang#generate": {
      "dependsOn": ["//#lang:extract"],
      "inputs": ["src/**/*.json", "package.json"],
      "outputs": ["dist/**"]
    },
    "@boluo/icons#generate": {
      "inputs": ["icons/**", "scripts/**", "package.json"],
      "outputs": ["src/**", "generated/**"]
    },
    "@boluo/grafana#build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    }
  }
}
