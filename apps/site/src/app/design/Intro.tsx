'use client';
import type { FC } from 'react';
import { FormattedMessage } from 'react-intl';

export const Intro: FC = () => {
  return <FormattedMessage defaultMessage="These are the pages used to design and debug the components" />;
};
