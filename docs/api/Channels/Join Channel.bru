meta {
  name: Join Channel
  type: http
  seq: 8
}

post {
  url: {{base_url}}/channels/join
  body: json
  auth: inherit
}

body:json {
  {
    "channelId": "{{channel_id}}",
    "characterName": "Link"
  }
}
