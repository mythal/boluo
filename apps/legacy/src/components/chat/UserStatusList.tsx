import * as React from 'react';
import { type Id } from '../../utils/id';

interface Props {
  spaceId: Id;
}

function MemberList(_props: Props) {
  return null;
}

export default React.memo(MemberList);
